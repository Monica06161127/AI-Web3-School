---
timezone: UTC+8
---

# 9-71

**GitHub ID:** 9-71

**Telegram:** @Q7v1a

## Self-introduction

AI x Web3 School

## Notes

<!-- Content_START -->
# 2026-05-19
<!-- DAILY_CHECKIN_2026-05-19_START -->
\# 📝 AI 时代的 Web3 开发者核心基础与架构能力

\> **Course Source:** LXDAO Tech Share via Bilibili / YouTube

\> **Topic:** 技术范式转变、Web2/Web3 混合支付架构、钱包密码学与 TEE 硬件安全设计

\> **Core Slogan:** 驾驭 AI，而非被 AI 驱动。让 AI 成为手脚，让自己成为大脑。

\---

\## 一、 技术范式的变与不变

\### 1. 人机协作 VN 模型

AI 爆发并没有降低系统复杂度，反而拉高了对架构师底层功底的要求。如果开发者底子不扎实，\*\*根本无法判断 AI 端上桌的代码是“屎山”还是“珍馐”\*\*。

\* **人类（大脑）：** 方案设计、合理性评估、代码最终验收、疑难 Bug 分析（承担最终系统责任）。

\* **AI（手脚）：** 编写样板代码、查阅官方标准、绘制简单数据流/请求流（担当高效率执行专家）。

\* **关键公式：** `AI 编码质量 ∝ AI 自我验证能力`（即通过死循环让 AI 自行编译、跑通测试、纠错）。

\### 2. 核心结论

\* **Web3 离不开 Web2：** 绝大多数 Web3 项目（如 CEX、DEX 聚合器、DeFi 前端）的 Web2 后端状态管理业务占比极高。光有智能合约，项目无法运转。

\* **安全源于设计：** Web3 是黑暗森林，没有 Web2 的银行初审、冻结与回滚（Chargeback）机制。出错误转即意味着资产直接归零，安全必须贯穿生命周期，审计只是锦上添花。

\---

\## 二、 实战拆解：从 Web2 支付到 Web3 稳定币支付系统

通过设计一个支持 `USDC` 的去中心化充值清结算系统，梳理底层技术架构。

\### 1. 架构流转演进

\#### 传统 Web2 支付链条

\`\`\`text

\[用户\] --(发起支付)--> \[电商平台\] --(创建订单)--> \[支付服务 TC\] <===> \[银行/第三方金融机构\] (记账与滑扣)

\`\`\`

\#### 升级后 Web3 稳定币支付链条

\`\`\`text

\[用户\] --(Off-chain 算法生成地址)--> \[专属 Deposit 钱包\]

|

(链上真实转账)

v

\[支付服务 TC\] <--(Callback 通知)-- \[链上监听服务\] <--(轮询/监听)-- \[Blockchain 账本\]

\`\`\`

\### 2. 核心架构差异对比

| 维度 | Web2 支付架构 | Web3 支付架构 |

| :--- | :--- | :--- |

| **信任底座** | 依赖中心化权威（银行/网关） | 依赖去中心化密码学共识 |

| **状态变更** | 银行数据库内部账户字段的滑扣 | 链上节点广播与区块打包（不可篡改） |

| **身份校验** | API Key / Secret Key / 支付密码 / MFA | 独立私钥生成的数字签名（Signature） |

| **异常处理** | 支持调账、冻结、恶意拒付（Chargeback） | 链上确定性清算，一旦转错完全不可逆 |

\---

\## 三、 核心底功：钱包密码学与交易生命周期

\### 1. 钱包的本质`Private Key -> Signature` 转换器

区块链底层\*\*“只认私钥不认人”\*\*。私钥本质上是一串巨大的随机数，钱包则是管理该私钥并输出签名的黑盒。

\* **交易签名（Transaction Sign）：** 证明用户的链上资产流转意图。

\* **消息签名（Message Sign）：** 证明用户的链上身份。

\* _机制：_ 后端生成一个随机消息（Nonce）传给前端 -> 用户用私钥签名 -> 后端利用公钥/地址解签。整个过程\*\*不需要知道用户的私钥\*\*，即可完成去中心化登录认证。

\### 2. 钱包分类

\* **EOA 钱包 (Externally Owned Account):** 原生私钥控制钱包。

\* _MPC 多方签名:_ 将私钥切片（Share）为多份（如 `7`, `8`, `9`），通过数学算法和 ZKP 协同签名，消灭单点泄漏风险。

\* **合约钱包 (CA / Smart Contract Account):** 由链上代码控制逻辑的钱包（如 Gnosis Safe 多签）。

\* **全托管钱包 (Custodial):** 类似 CEX（交易所），私钥存在服务器，用户通过 Web2 账密交互。

\* **混合托管 (Hybrid):** 用户持有一片私钥，服务商持有另一片，双方协同才能完成多签。

\### 3. 主流公链交易防重放机制差异

当向区块链提交交易时，不同链确保交易幂等性（防止黑客重复提交同一笔交易）的底层逻辑截然不同：

\* **EVM 生态 (Ethereum/Base/Polygon):** 账户内置递增的 `Nonce`（0, 1, 2...）。同一 Nonce 只能成功打包一次，若 Nonce 冲突则后续交易无法提交。

\* **波场与 Solana 生态 (Tron/Solana):** 原生无 Nonce 概念。SDK 内部默认引入 `Latest Block Hash`（最近几个区块的哈希值）。交易绑定该特定哈希，过期自动失效，防止被无限重放。

\* **比特币生态 (BTC):** 采用 `UTXO`（未花费交易输出）模型。天然是“纸币找零”逻辑，一个 UTXO 只要被消费过一次，该“纸币”即作废，物理上杜绝了重放。

\---

\## 四、 高阶架构能力：服务端钱包防御设计

在处理高频、大额的平台级资金流转（如交易所充提、支付网关）时，生产环境必须落地的两项核心架构能力：

\### 1. 预签名阶段的“交易模拟 (Transaction Simulation)”

为了防御 EIP-712 可视化签名依然无法完全规避的恶意钓鱼假消息，必须在\*\*最终签名生成之前\*\*引入模拟节点。

\`\`\`text

\[构造交易 Data\] -> \[发送至仿真节点(如模拟 EVM)\] -> \[解析账户资产变动预期\] -> \[前端可视化呈现确认\] -> \[通过后才允许人类/AI调用私钥签名\]

\`\`\`

_⚠️_ \*核心铁律：\*\* 绝对不能在签名生成后再做模拟流转，否则签名一旦在系统中暴露，就会增加极大的黑客截获风险。

\### 2. 硬件级：TEE (可信执行环境) 签名架构

为了彻底根除私钥明文泄漏在服务器内存或日志中的隐患，大资管系统必须采用基于 TEE（如 Intel SGX、AWS Nitro Enclaves）的绝缘硬件隔离签名：

\`\`\`text

(服务器普通云环境) | (TEE 绝缘芯片/内存环境)

|

\[加密的私钥片段\] + \[待签名交易\] ----------+--> \[ 解密私钥密文 (内存隔离，外部无法嗅探) \]

| |

| v

\[输出最终数字签名 (Signature)\] <----------+--------- \[ 芯片内执行执行密码学签名 \]

\`\`\`

\* **技术优势：** 私钥明文自始至终在 TEE 物理内存内部流转，任何系统进程、高级 Root 权限都无法跨区域窥探，输入是密文，输出是签名。

\---

\## 五、 个人进阶 Roadmap

\`\`\`text

⭐ 驾驭 AI 架构师

├── 高阶防御能力 (TEE 隔离、交易前 Simulation 仿真架构设计)

├── 跨链底层差异 (深入 EVM Nonce 调度、Solana 块哈希、BTC UTXO 资产路由)

└── 坚实的密码学地基 (深入 ECDSA/EDDSA、MPC 签名原理、EIP-4337 账户抽象)

\`\`\`
<!-- DAILY_CHECKIN_2026-05-19_END -->
<!-- Content_END -->
